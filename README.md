# odin-form-validation-practice
Practice creating a form with mulitple fields and validation checks. Invalid fields trigger custom error messages in the DOM rather than using the browser's built in validation UI.
